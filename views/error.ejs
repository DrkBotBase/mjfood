<!DOCTYPE html>
<html lang="es" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name_page %></title>
    
    <!-- PWA Meta Tags -->
    <meta name="description" content="<%= message %>">
    <meta name="theme-color" content="<%= info.colorBase.bg %>">
    
    <meta name="robots" content="noindex, nofollow">
    <meta name="googlebot" content="noindex, nofollow">
    <link rel="canonical" href="<%= info.dominio %>">
    
    <meta property="og:title" content="<%= name_page %>">
    <meta property="og:description" content="<%= message %>.">
    
    <meta property="og:image" content="/assets/<%= info.logo %>.jpg">
    <meta property="og:image:alt" content="<%= name_page %>">
    <meta property="fb:app_id" content="">
    <meta property="og:url" content="<%= info.dominio %>">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="<%= name_page %>">
    <meta property="og:locale" content="es_ES">
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= name_page %>">
    <meta name="twitter:description" content="<%= message %>">
    <meta name="twitter:image" content="/assets/<%= info.logo %>.jpg">
    <meta name="twitter:image:alt" content="<%= name_page %>">
    
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="shortcut icon" href="/assets/icon.ico" type="image/x-icon">
    <link rel="icon" type="image/png" href="/assets/icon.png">
    <link rel="apple-touch-icon" href="/assets/icon.png">
    
    <!-- Tailwind CSS -->
    <link rel="stylesheet" href="/css/output.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css" integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
      :root {
        --text-color: <%= info.colorBase.text %>;
        --primary-color: <%= info.colorBase.primary %>;
        --bg-color: <%= info.colorBase.bg %>;
        --light-shadow: <%= info.colorBase.light %>;
        --dark-shadow: <%= info.colorBase.dark %>;
      }
      body {
        font-family: 'Inter', sans-serif;
        background-color: var(--bg-color);
        color: var(--text-color);
      }
      .neumorphic-flat {
        background: var(--bg-color);
        box-shadow: 7px 7px 15px var(--dark-shadow), -7px -7px 15px var(--light-shadow);
      }
      .neumorphic-button {
        background: var(--bg-color);
        box-shadow: 5px 5px 10px var(--dark-shadow), -5px -5px 10px var(--light-shadow);
        transition: all 0.2s ease-in-out;
      }
      .neumorphic-button:hover {
        box-shadow: 3px 3px 7px var(--dark-shadow), -3px -3px 7px var(--light-shadow);
      }
      .neumorphic-button:active {
        box-shadow: inset 3px 3px 7px var(--dark-shadow), inset -3px -3px 7px var(--light-shadow);
      }
      .accent-text {
        color: var(--primary-color);
      }
    </style>
</head>
<body class="antialiased">
    <!-- Header -->
    <header class="sticky top-0 z-50">
        <div class="container mx-auto px-6 py-4">
             <div class="neumorphic-flat w-full rounded-2xl p-4 flex justify-center items-center">
                <a href="/" class="text-2xl font-bold tracking-tighter">Menú<span class="accent-text">Digital</span></a>
            </div>
        </div>
    </header>

    <!-- Header -->
    <main>
      <!-- 404 Section -->
      <section class="min-h-[70vh] flex items-center justify-center text-center px-6">
        <div>
          <h1 class="text-8xl md:text-9xl font-extrabold tracking-tighter accent-text">404</h1>
          <h2 class="text-3xl md:text-4xl font-bold tracking-tight mt-4 mb-2"><%= message %></h2>
          <p class="text-lg max-w-md mx-auto text-gray-600 mb-8">
            ¡Ups! Parece que el plato que buscas no está en nuestro menú. Regresemos a la cocina principal.
          </p>
          
          <% if (restaurantesDisponibles && restaurantesDisponibles.length > 0) { %>
          <div class="grid grid-cols-2 gap-3 mb-8">
            <% restaurantesDisponibles.forEach(rest => { %>
            <a href="<%= rest.id %>" class="neumorphic-button font-bold py-4 px-10 rounded-xl text-lg">
              <%= rest.nombre %>
            </a>
            <% }); %>
          </div>
          <% } %>
          <a href="/lista" class="neumorphic-button inline-block text-white font-bold py-4 px-10 rounded-full text-lg" style="background-color: var(--primary-color);">
            Volver al Inicio
          </a>
        </div>
      </section>
    </main>
    
    <!-- Footer -->
    <footer class="pt-10 pb-8">
      <div class="container mx-auto px-6 text-center">
        <div class="border-t border-gray-300 pt-8 text-gray-500">
          <p>&copy; 2024 MenúDigital. Todos los derechos reservados.</p>
        </div>
      </div>
    </footer>
  </body>
</html>